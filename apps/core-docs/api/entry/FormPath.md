# FormPath

请参考 [`@silver-formily/path` 的文档](https://path.silver-formily.org/)，`@silver-formily/core` 完全借用并重新导出了其内部的 `Path` 类。

:::warning 注意
如果你从 `@silver-formily/path` 中引入，则类的名称是 `Path`，从`@silver-formily/core`中引入，则类的名称是 `FormPath`。两者虽然是等价的但是导出的名称不同。
:::
