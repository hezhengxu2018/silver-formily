# Form Validator Registry

完整文档请参考 `@silver-formily/validator` 中的 [注册中心​](https://validator.silver-formily.org/api/registry)章节。

`@silver-formily/core` 完全借用并重新了导出了 `@silver-formily/validator`。两者的能力是一致的。
