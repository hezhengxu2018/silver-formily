# Space

> Element Plus flex layout component

::: warning Note
This project does not wrap `Space` in any custom way. It simply re-exports `ElSpace` from peer dependencies, so the behavior is the same as using `ElSpace` directly.
:::

## Markup Schema Example

:::demo

space/markup-schema

:::

## JSON Schema Example

:::demo

space/json-schema

:::

## Template Example

:::demo

space/template

:::

## API

See [https://element-plus.org/en-US/component/space.html](https://element-plus.org/en-US/component/space.html)
